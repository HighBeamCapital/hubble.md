[package]
name = "hubble"
version = "0.1.17"
edition = "2021"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2" }

[dependencies]
tauri = { version = "2" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]
